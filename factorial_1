def factorial(numero):
    fac=1
    for i in range(numero):
        fac=fac*(i+1)
    return fac
        
sistema=1
while sistema==1:
    numero=input("Ingrese un número: ")
    validar=numero.isdigit()
    if validar==True:
        print(factorial(int(numero)))
        sistema=0
